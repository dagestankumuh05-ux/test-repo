/**
 * ecosystem.config.cjs
 * Конфигурация PM2 для production-запуска.
 *
 * Использование:
 *   pm2 start ecosystem.config.cjs
 *   pm2 save          # сохранить список процессов
 *   pm2 startup       # автозапуск при перезагрузке сервера
 */

module.exports = {
  apps: [
    {
      // ── Идентификация ────────────────────────────────────────────────────
      name:        'market-bot',
      script:      'index.js',

      // ── Интерпретатор ────────────────────────────────────────────────────
      // node_args нужен если PM2 старый и не понимает ESM
      // node_args:   '--experimental-modules',
      interpreter: 'node',

      // ── Переменные окружения ─────────────────────────────────────────────
      // PM2 подхватит .env автоматически если задан env_file
      // Либо можно прописать переменные прямо здесь (не рекомендуется)
      env: {
        NODE_ENV:  'production',
        LOG_LEVEL: 'info',
      },
      env_development: {
        NODE_ENV:  'development',
        LOG_LEVEL: 'debug',
      },

      // ── Автоперезапуск ───────────────────────────────────────────────────
      autorestart:  true,           // перезапускать при краше
      max_restarts: 10,             // максимум 10 авто-рестартов
      min_uptime:   '30s',          // считать стабильным если прожил >30с
      restart_delay: 5000,          // ждать 5с перед рестартом

      // ── Memory guard ─────────────────────────────────────────────────────
      max_memory_restart: '256M',   // рестарт если > 256 МБ

      // ── Логи ─────────────────────────────────────────────────────────────
      error_file:    './logs/pm2-error.log',
      out_file:      './logs/pm2-out.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      merge_logs:    true,

      // ── Опции ────────────────────────────────────────────────────────────
      watch:         false,         // не перезапускать при изменении файлов
      instances:     1,             // один экземпляр (бот не нуждается в кластере)
      exec_mode:     'fork',
    },
  ],
};
